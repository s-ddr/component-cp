config:
  target: 'http://localhost:3002'
  phases:
    - duration: 10
      arrivalRate: 1
      rampTo: 5
    - duration: 50
      arrivalRate: 5
  processor: "./createTestID.js"
scenarios:
  - flow:
    - loop:
      - function: "createTestID"
      - get:
          url: "/collection/mongo/{{id}}"
      # - log: "get {{id}}"
      count: 400